# Keep the SQLite database and its journal/sidecar files private.
# Apache 2.4+
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

# Apache 2.2 / mod_access_compat fallback
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
